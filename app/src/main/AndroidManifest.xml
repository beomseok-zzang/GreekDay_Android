<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.beomseok.greekday">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name=".GreekDayApplication"
        android:allowBackup="true"
        android:icon="@mipmap/greekday_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/MyMaterialTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".CounselActivity"
            android:theme="@style/MyMaterialTheme" />
        <activity android:name=".OrderToppingActivity" />
        <activity
            android:name=".NoticeActivity"
            android:theme="@style/MyMaterialTheme" />
        <activity
            android:name=".CardActivity"
            android:theme="@style/MyMaterialTheme" />
        <activity android:name=".BaseDetailActivity" />
        <activity
            android:name=".OrderActivity"
            android:theme="@style/MyMaterialTheme" />
        <activity android:name=".CartActivity" />
        <activity
            android:name=".NoticeDetailActivity"
            android:theme="@style/MyMaterialTheme" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LastQuestionActivity" />
        <activity android:name=".SelectNickNameActivity"></activity>
    </application>

</manifest>